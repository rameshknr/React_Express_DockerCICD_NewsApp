image: node
stages:
  - build
  - test
build:
  stage:
    - build
  script:
    - cd NewsApp_UI
    - 'npm install'
    - 'npm run build'
test:
  image: newsui
  stage:
    - test
  include:
    - local: /NewsApp_UI
  script:
    - cd NewsApp_UI
    - 'npm install'
    - 'npm run test'