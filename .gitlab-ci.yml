image: node
stages:
  - build
  - test
include: 'docker-compose.yml'
build:
  stage:
    - build
  script:
    - 'npm install'
    - 'npm run build'
test:
  image: newsui
  stage:
    - test
  script:
    - 'npm install'
    - 'npm run test'